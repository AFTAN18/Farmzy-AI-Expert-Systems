# FARMZY Standalone ML Pipeline

## Run

```bash
pip install -r requirements.txt
python train_pipeline.py
```

Artifacts are generated in `ml_artifacts/`.
Copy generated `.joblib` files to `farmzy-backend/ml/artifacts/`.
